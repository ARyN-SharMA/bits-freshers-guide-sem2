import type React from "react"
import type { Metadata } from "next"
import { Geist, Geist_Mono } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"

const _geist = Geist({ subsets: ["latin"] })
const _geistMono = Geist_Mono({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "BITS Fresher's Guide | II Semester 2025-26",
  description:
    "Your complete roadmap to conquering second-semester courses at B.I.T.S. Pilani with instructor insights, ratings, and a timetable generator.",
  keywords: ["BITS Pilani", "Course Registration", "Timetable", "Fresher Guide", "2025-26"],
    generator: 'v0.app'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`font-sans antialiased`}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
